import React from 'react';

export function Providers({ children }: { children: any }) {
  return children;
}

// Add default export to prevent Metro from trying to route this file
export default Providers;
