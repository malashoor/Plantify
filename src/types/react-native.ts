import type {
  ViewProps,
  TextProps,
  TouchableOpacityProps,
  ScrollViewProps,
  StyleProp,
  ViewStyle,
  TextStyle,
  ColorSchemeName,
} from 'react-native';

export type {
  ViewProps,
  TextProps,
  TouchableOpacityProps,
  ScrollViewProps,
  StyleProp,
  ViewStyle,
  TextStyle,
  ColorSchemeName,
}; 